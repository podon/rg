---
driver:
  name: vagrant
  network:
  - ["private_network", { ip: "172.16.1.111" }]
  - ["forwarded_port", {guest: 80, host: 80}]

provisioner:
  name: chef_solo

platforms:
  - name: centos-7.1

suites:
  - name: default
    run_list:
      - recipe[rg::default]

